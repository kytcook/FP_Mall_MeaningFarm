<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.NoticeMapper">

	<select id="list" resultType="com.meaningfarm.mall.notice.domain.NoticeVO">
		select * 
		from notice 
		order by notice_no desc
	</select>
	
   
    <!-- 작성 -->
	<insert id="register">
		insert into NOTICE (NOTICE_NO, NOTICE_TITLE, ADMIN_NAME,  NOTICE_CONTENT)
    		values (SEQ_NOTICE_NO.NEXTVAL, #{NOTICE_TITLE}, #{ADMIN_NAME}, #{NOTICE_CONTENT})
	</insert>

</mapper>